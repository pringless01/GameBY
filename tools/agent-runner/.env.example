# OpenAI / Model
OPENAI_API_KEY=sk-...

# Model seçimi - görev tipine göre otomatik seçilir
MODEL_LIGHT=gpt-4o-mini    # Gündelik işler: hafıza güncelleme, basit commit'ler, rapor yazma
MODEL_HEAVY=gpt-4o         # Ağır işler: kod analizi, refactoring, kompleks planlama  
MODEL_DEFAULT=gpt-4o-mini  # Varsayılan model (geriye dönük uyumluluk)

MAX_IDLE_SECONDS=120   # watchdog süresi

# (Opsiyonel) GitHub CLI token, genelde gh login yeter
GH_TOKEN=
