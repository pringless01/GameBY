{
  "name": "gameby-root",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "codex:run": "node tools/codex-runner.js",
    "lint": "npm --prefix apps/api run lint",
    "lint:fix": "npm --prefix apps/api run lint:fix",
    "test": "npm --prefix apps/api test",
    "coverage": "npm --prefix apps/api run coverage",
    "ci:all": "npm --prefix apps/api test"
  }
}
