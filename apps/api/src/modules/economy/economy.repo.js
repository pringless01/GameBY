// Placeholder repo for Economy domain. DB access remains in legacy service.
// Intentionally minimal to avoid behavior change. Future: move DB queries here.
export const _placeholder = true;
export async function loadEconomyData(_db) { return {}; }
